import 'package:flutter/material.dart';




class CircleViewListIndex extends StatelessWidget {
  const CircleViewListIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Extra Screen'),
      ),
      // body: const ScrollingCustomShape(),
    );
  }
}
